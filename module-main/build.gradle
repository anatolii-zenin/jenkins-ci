plugins {
    id 'application'
    id "jacoco-report-aggregation"
    id "test-report-aggregation"
    id 'org.springframework.boot' version "$bootVersion"
    id 'war'
}

mainClassName = 'com.mjc.school.main.Main'

dependencies {
    implementation project(':module-web')
}